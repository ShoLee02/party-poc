/*  
    Esta carpeta contiene funciones de utilidad general que no están 
    directamente relacionadas con la lógica de la aplicación, pero 
    que pueden ser útiles en múltiples lugares, como funciones de 
    manipulación de datos, formateo, etc.

    // utils/validateEmail.ts
    export const validateEmail = (email: string): boolean => {
        // Expresión regular para validar el formato del correo electrónico
        const emailRegex = /\S+@\S+\.\S+/;
        return emailRegex.test(email);
    };
*/